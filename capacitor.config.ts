import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'fhv.dwa.keyonic',
  appName: 'keyonic',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
