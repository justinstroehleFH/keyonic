export type Password = {
  id: string;
  title: string;
  username: string;
  password: string;
  url: string;
  label: string[];
};

export type Label = {
  labelName: string;
  icon: string;
};

export type LabelURL = {
  labelName: string;
  icon: string;
  url: string;
};
